/**
 * Default-Daten — re-export aus dem lokalen Schema-Paket.
 * Ersetzt die alte defaultData.js mit hartcodierten Feldern.
 */
import { DEFAULT_JAHRESABSCHLUSS } from '../../../packages/schema/src/index';

export const DEFAULT_DATA = DEFAULT_JAHRESABSCHLUSS;
